# 命令列表

ChatAI Plugin 提供了丰富的命令来控制 AI 对话、管理群组设置和执行各种功能。
本文命令均从 `apps/` 下的注册正则（`reg`）核对而来；另外 `#ai帮助` 的帮助图片
命令清单由 `apps/Management.js` 的 `help()` 内联 `commandCategories` 渲染（与注册
正则存在少量文案差异，以注册正则为准）。

::: tip 命令前缀
默认命令前缀为 `#ai`，可在配置中修改。下文中 `{prefix}` 代表你配置的命令前缀。
:::

## 💬 对话命令

基础的对话管理命令，所有用户可用。注册于 `apps/Commands.js`。

| 命令 | 注册正则（源码） | 说明 |
|:-----|:-----|:-----|
| `#结束对话` / `#结束会话` / `#新对话` / `#新会话` | `^#(结束对话\|结束会话\|新对话\|新会话)$` | 结束当前会话/开新对话 |
| `#clear` | `^#clear$` | 清除上下文（与结束对话并存） |
| `#清除记忆` / `#清理记忆` / `#删除记忆` | `^#(清除记忆\|清理记忆\|删除记忆)$` | 清除个人记忆数据 |
| `#对话状态` / `#会话状态` | `^#(对话状态\|会话状态)$` | 查看当前对话详细状态 |
| `#我的记忆` / `#查看记忆` / `#记忆列表` | `^#(我的记忆\|查看记忆\|记忆列表)$` | 查看已保存的记忆列表 |
| `#总结记忆` / `#记忆总结` / `#整理记忆` | `^#(总结记忆\|记忆总结\|整理记忆)$` | 整理合并记忆条目 |
| `#chatdebug [on\|off]` | `^#chatdebug\s*(true\|false\|on\|off\|开启\|关闭)?$` | 切换聊天调试模式（缺省参数时切换） |
| `#工具模式 [ask/auto/confirm_all/yolo]` | `^#工具模式(?:\s+(ask\|询问\|auto\|自动\|confirm_all\|全确认\|yolo\|直接执行))?$` | 查询/设置工具审批模式（支持中文别名） |

## 👥 群聊功能

群聊相关的功能命令，注册于 `apps/Commands.js`。

| 命令 | 注册正则（源码） | 说明 |
|:-----|:-----|:-----|
| `#群聊总结` / `#总结群聊` / `#群消息总结` / `#画像总结`（可带 `2`） | `^#(群聊总结\|总结群聊\|群消息总结\|画像总结)(2)?$` | AI 总结近期群聊内容（带 2 为旧版风格） |
| `#今日群聊` / `#群聊总结2` / `#现代总结` | `^#(今日群聊\|群聊总结2\|现代总结)$` | 现代风格群聊总结 |
| `#个人画像` / `#用户画像` / `#分析我` | `^#(个人画像\|用户画像\|分析我)(?:\s+.*)?$` | 分析用户个人画像 |
| `#画像`（前缀） | `^#画像` | 回复用户消息分析指定用户画像（`#画像@用户`） |
| `#今日词云` / `#词云` / `#群词云` | `^#(今日词云\|词云\|群词云)$` | 生成群聊词云图 |
| `#群记忆` / `#群聊记忆` | `^#(群记忆\|群聊记忆)$` | 查看群聊共享记忆 |

## 🎨 绘图功能

AI 绘图相关命令，由 `apps/ImageGen.js` 的命令规则表处理，支持带 `#` 或不带 `#`。

| 命令 | 源码规则 | 说明 |
|:-----|:-----|:-----|
| `文生图 <描述>` | `/^#?文生图\s*(.+)$/` | 文生图 |
| `图生图` | `/^#?图生图\s*(.*)$/` | 回复图片生成 |
| `文生视频` / `图生视频` | `/^#?文生视频\s*(.+)$/`、`/^#?图生视频\s*(.*)$/` | 视频生成 |
| `画 <描述>` | 预设/自定义前缀规则（`presetHandler` 与 `customPrefixDraw`） | 按预设关键词绘图 |
| `手办化` / `Q版` / `动漫化` / `赛博朋克` / `油画` / `水彩` | 预设匹配规则 | 图片风格转换（回复图片发送） |
| `画图状态` / `谷歌状态` / `api状态` | `/^#?(谷歌状态\|画图状态\|api状态)$/` | 绘图 API 状态 |
| `绘图帮助` / `画图帮助` | `/^#?(绘图帮助\|画图帮助\|绘图帮助)$/` | 绘图帮助 |
| `绘图模型 <name>` / `设置绘图模型` | `/^#?(绘图模型\|画图模型\|设置绘图模型\|切换绘图模型)\s*(.*)$/` | 切换绘图模型 |
| `更新预设` / `刷新预设` | `/^#?(更新预设\|更新焚决\|刷新预设\|重载预设)$/` | 远程预设刷新 |

## 🎮 Galgame 游戏

AI 驱动的 Galgame 文字冒险游戏，注册于 `apps/Galgame.js`（`{prefix}` 前缀）。

| 命令 | 注册正则（源码） | 说明 |
|:-----|:-----|:-----|
| `{prefix}游戏开始 [角色]` | `^{prefix}游戏\s*开始(\s+\S+)?$` | 开始新的 Galgame 冒险 |
| `{prefix}游戏状态` | `^{prefix}游戏\s*状态$` | 查看当前游戏状态 |
| `{prefix}游戏退出` | `^{prefix}游戏\s*退出$` | 暂时退出游戏 |
| `{prefix}游戏结束` | `^{prefix}游戏\s*结束$` | 结束并清空数据 |
| `{prefix}游戏导出` / `游戏导出对话` | `^{prefix}游戏\s*导出(对话)?$` | 导出存档 |
| `{prefix}游戏导入` | `^{prefix}游戏\s*导入$` | 导入存档 |
| `{prefix}游戏角色列表` / `创建角色` / `删除角色` | `^{prefix}游戏\s*(角色列表\|创建角色\|删除角色\s+\S+)$` | 角色管理 |
| `{prefix}游戏背包` / `物品` / `道具` | `^{prefix}游戏\s*(背包\|物品\|道具)$` | 查看背包 |
| `{prefix}游戏日常` / `探索` / `任务` / `商店` / `打工` | 对应正则（同一行内多命令） | 游戏内玩法 |
| `{prefix}游戏编辑` / `游戏在线编辑` | `^{prefix}游戏\s*(在线)?编辑$` | 生成在线编辑入口 |
| `{prefix}游戏帮助` | `^{prefix}游戏\s*帮助$` | 查看游戏帮助 |

> 注意：旧版命令 `#游戏继续`、`#游戏存档` 在注册正则中不存在。

## 🎭 人格设定

自定义 AI 人格的命令，注册于 `apps/Management.js`。

| 命令 | 注册正则（源码） | 权限 |
|:-----|:-----|:-----|
| `{prefix}设置人格 <内容>` | `^{prefix}设置人格\s+` | 所有用户 |
| `{prefix}设置群人格 <内容>` | `^{prefix}设置群人格\s+` | 主人 |
| `{prefix}查看人格` | `^{prefix}查看人格$` | 所有用户 |
| `{prefix}清除人格` | `^{prefix}清除人格$` | 所有用户 |
| `{prefix}清除群人格` | `^{prefix}清除群人格$` | 主人 |

::: info 人格优先级
人格配置的优先级为：**群内用户 > 群组 > 用户全局 > 默认预设**
:::

## ⚙️ 群管理命令

群组管理相关命令，注册于 `apps/Management.js` / `apps/Commands.js`。

| 命令 | 注册正则（源码） | 权限 |
|:-----|:-----|:-----|
| `#群管理面板` / `#群管理入口` / `#群设置面板` | `^#(群管理面板\|群管理入口\|群设置面板)$`（Commands.js） | 群管理员 |
| `{prefix}群伪人开启/关闭` | `^{prefix}群伪人(开启\|关闭)$` | 群管理员 |
| `{prefix}群绘图开启/关闭` | `^{prefix}群绘图(开启\|关闭)$` | 群管理员 |
| `{prefix}群设置` | `^{prefix}群设置$` | 群管理员 |

## 📡 群渠道与限制

注册于 `apps/Management.js`。

| 命令 | 注册正则（源码） | 权限 |
|:-----|:-----|:-----|
| `{prefix}群渠道设置` | `^{prefix}群渠道设置$` | 群管理员 |
| `{prefix}群渠道开启独立` / `关闭独立` | `^{prefix}群渠道(开启\|关闭)独立$` | 群管理员 |
| `{prefix}群禁用全局` / `群启用全局` | `^{prefix}群(禁用\|启用)全局$` | 群管理员 |
| `{prefix}群限制设置` | `^{prefix}群限制设置$` | 群管理员 |
| `{prefix}群限制 <群限> <用户限>` | `^{prefix}群限制\s+(\d+)\s*(\d*)$` | 群管理员 |
| `{prefix}群使用统计` | `^{prefix}群使用统计$` | 群管理员 |
| `{prefix}群重置统计` | `^{prefix}群重置统计$` | 群管理员 |

::: tip 使用限制示例
`#ai群限制 100 20` 表示设置群每日 100 次，每用户每日 20 次的使用限制。
:::

## 👑 主人命令

仅限 Bot 主人使用的管理命令，注册于 `apps/Management.js`。

| 命令 | 注册正则（源码） | 说明 |
|:-----|:-----|:-----|
| `{prefix}管理面板` | `^{prefix}管理面板$` | 获取 Web 管理面板（临时链接） |
| `{prefix}管理面板 永久` | `^{prefix}管理面板\s*永久$` | 获取 Web 管理面板（永久链接） |
| `{prefix}结束对话` | `^{prefix}结束对话$` | 结束对话（管理侧处理） |
| `{prefix}结束全部对话` | `^{prefix}结束全部对话$` | 清除所有用户的对话 |
| `{prefix}状态` | `^{prefix}状态$` | 查看插件运行状态 |
| `{prefix}调试开启/关闭` | `^{prefix}调试(开启\|关闭)$` | 切换调试模式 |
| `{prefix}伪人开启/关闭` | `^{prefix}伪人(开启\|关闭)$` | 切换全局伪人模式 |
| `{prefix}设置模型 <名称>` / `设置model` | `^{prefix}设置(模型\|model)\s*(.+)$` | 设置默认使用的模型 |
| `{prefix}帮助` | `^{prefix}帮助$` | 命令帮助图片 |

## 🔄 版本更新

插件版本管理命令，注册于 `apps/Update.js`。

| 命令 | 注册正则（源码） | 说明 | 权限 |
|:-----|:-----|:-----|:-----|
| `#ai版本` | `^#ai版本$` | 查看当前版本信息 | 所有用户 |
| `#ai检查更新` | `^#ai检查更新$` | 检查是否有新版本 | 主人 |
| `#ai更新` | `^#ai(强制)?更新$` | 更新插件到最新版 | 主人 |
| `#ai强制更新` | `^#ai(强制)?更新$` | 强制更新（覆盖本地修改） | 主人 |
| `#ai更新日志` | `^#ai更新日志$` | 查看提交历史 | 主人 |

> 注意：`#ai更新` 与 `#ai强制更新` 共用同一条正则 `^#ai(强制)?更新$`。

## 🛠️ 消息检查命令（MessageInspector）

| 命令 | 注册正则（源码） | 说明 |
|:-----|:-----|:-----|
| `#取` / `#取123` / `#取上5条` / `#取$id` / `#取消息` | `^#取(\d*)$`、`^#取上(\d+)条?$`、`^#取\$([^\s]+)$`、`^#取消息(\d*)$` | 获取消息详情/序号取消息 |
| `#消息详情` | `^#消息详情(\d*)$` | 查看消息详情 |
| `#统计` / `#ai统计` / `#统计详情` | `^#(ai)?统计$`、`^#(ai)?统计详情$` | 查看统计 |
| `#重置统计` / `#ai重置统计` | `^#(ai)?重置统计$` | 重置统计 |
| `#debug` / `#aidebug信息` | `^#(ai)?debug(信息)?$` | 调试信息 |

## 🖼️ 表情小偷（EmojiThief）

| 命令 | 注册正则（源码） | 说明 |
|:-----|:-----|:-----|
| `偷图` / `偷表情` / `保存这个` / `存图` / `save` / `偷` | `^(偷图\|偷表情\|保存这个\|保存图片\|存图\|存表情\|save\|偷)$` | 保存图片为表情（第二条规则为空，按角色判断） |

## 🤖 触发方式

除了命令触发外，还支持以下触发方式与 AI 对话：

### @触发
直接 @机器人 并发送消息即可触发对话。

### 前缀触发

触发前缀优先读 `trigger` 配置（`trigger.prefixes`）；旧版 `listener` 配置未迁移时
退化为 `listener.triggerPrefix`（默认数组 `['#chat']`，见 `apps/chat.js` 的
`getTriggerConfig()`）。这里的 `{prefix}`（默认 `#ai`）是命令前缀，与上述对话
触发前缀不是同一配置；`#` 开头的一组（`#结束对话` 等）是独立注册的固定前缀命令。

```
#chat 你好
```

### 关键词触发
在消息中包含配置的关键词（如机器人昵称）会触发对话。

### 随机触发
在群聊中有一定概率随机触发回复（伪人模式）。

## 📝 使用示例

### 基础对话
```
@机器人 今天天气怎么样？
#chat 帮我写一首诗
```

### AI 绘图
```
画 一只戴着墨镜的柯基在海边冲浪
```

### 图片风格转换
1. 发送一张图片
2. 回复该图片并发送 `手办化`

### 群聊总结
```
#群聊总结
#今日词云
```

### 设置人格
```
#ai设置人格 你是一个傲娇的猫娘，说话时喜欢在句末加上"喵~"
```

---

::: tip 获取帮助
发送 `#ai帮助` 可以在 Bot 中查看命令帮助图片。
:::
